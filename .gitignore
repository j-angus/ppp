# Lines that start with '#' are comments.
# For a project mostly in C, the following would be a good set of
# exclude patterns:

# ignore objects and archives, anywhere in the tree.
*.[oa]
# compiled binaries 
bin/
# book source code directory
code/
